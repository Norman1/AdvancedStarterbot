package bot;

import java.util.ArrayList;
import java.util.List;

/**
 * HistoryTracker is responsible for storing the whole history of what has
 * happened on the board so far and our thoughts about it.
 * 
 */
public class HistoryTracker {

	public static BotState botState;

	// private static List<BotState> botStates = new ArrayList<>();

	// public static void addState(BotState state) {
	// botStates.add(state);
	// }

	// public static BotState getState() {
	// return botStates.get(botStates.size() - 1);
	// }

	// public static BotState getState(int index) {
	// return botStates.get(index);
	// }

}
